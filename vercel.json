{
  "framework": "nextjs",
  "buildCommand": "prisma generate && node scripts/ensure-database.mjs && next build",
  "git": {
    "deploymentEnabled": {
      "master": true,
      "main": true
    }
  }
}
