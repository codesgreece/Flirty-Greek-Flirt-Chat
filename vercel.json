{
  "framework": "nextjs",
  "buildCommand": "node scripts/ensure-database.mjs && prisma generate && prisma migrate deploy && prisma db seed && next build",
  "git": {
    "deploymentEnabled": {
      "master": true,
      "main": true
    }
  }
}
